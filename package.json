{
  "name": "dp-integrator",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted TypeScript/Azure-native replacement for Celigo's Shopify→NetSuite connector (multi-tenant, multi-NS-account).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "tsc -b tsconfig.json --clean && pnpm -r exec rm -rf dist .vitest-temp coverage"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "~5.5.4",
    "vitest": "^2.1.4",
    "@vitest/coverage-v8": "^2.1.4"
  }
}
