/**
 * Admin REST + CLI surface for replay (per brief M2 acceptance).
 *
 * M0 ships only the package skeleton + CLI stub. The REST handlers and the
 * `replay`/`list`/`quarantine` CLI verbs land in M2 alongside the SQL-backed
 * `ErrorStore`.
 */
export const placeholder =
  'M2 will register replay REST handlers + CLI verbs (replay, list, quarantine).';
